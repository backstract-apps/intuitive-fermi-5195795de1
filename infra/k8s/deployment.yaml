---
apiVersion: v1
kind: Secret
metadata:
  name: intuitive-fermi-5195795de1-bst-token
  namespace: apps-dev
  labels:
    app: intuitive-fermi-5195795de1
    managed-by: backstract
  annotations:
    backstract.io/token-scope: "storage.*,cache.*"
type: Opaque
stringData:
  token: "PLACEHOLDER_BST_TOKEN_REPLACE_AT_RUNTIME"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: intuitive-fermi-5195795de1-depl
  namespace: apps-dev
  labels:
    app: intuitive-fermi-5195795de1
    release: prometheus # Added label for PodMonitor discovery
spec:
  replicas: 0
  selector:
    matchLabels:
      app: intuitive-fermi-5195795de1
      release: prometheus # Added label for PodMonitor discovery
  template:
    metadata:
      labels:
        app: intuitive-fermi-5195795de1
        release: prometheus # Added label for PodMonitor discovery
    spec:
      serviceAccountName: alloy-service-account
      volumes:
      - name: alloy-config
        configMap:
          name: alloy-configmap
          items:
          - key: alloy.river
            path: config.alloy

      - name: podinfo
        downwardAPI:
          items:
          - path: "labels"
            fieldRef:
              fieldPath: metadata.labels
          - path: "annotations"
            fieldRef:
              fieldPath: metadata.annotations
      tolerations:
        - key: "workload"
          operator: "Equal"
          value: "user"
          effect: "NoSchedule"
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: nodepool
                    operator: In
                    values: ["spot-users"]
      containers:
      - name: intuitive-fermi-5195795de1
        image: 216084506783.dkr.ecr.ap-south-1.amazonaws.com/backstract_apps:intuitive-fermi-5195795de1
        imagePullPolicy: Always
        env:
        - name: BACKSTRACT_AUTH_TOKEN
          valueFrom:
            secretKeyRef:
              name: intuitive-fermi-5195795de1-bst-token
              key: token
        - name: BACKSTRACT_PLATFORM_URL
          value: "https://api.beemerbenzbentley.site/sigma"
        envFrom:
        - configMapRef:
            name: env-configmap-coll-8988b194455245ab92ba9aff8b6a795a-deployment-69f1b1dd-9f96-425c-b7cd-d275915a3f32
        - secretRef:
            name: app-secrets-coll-8988b194455245ab92ba9aff8b6a795a
        ports:
        - containerPort: 7070
          name: http # Optional for HTTP traffic
        - containerPort: 7070 #same as the deployed code
          name: metrics # Added port for metrics scraping
        resources:
          requests:
            memory: "210Mi"
            cpu: "200m"
          limits:
            memory: "500Mi"
            cpu: "500m"

      - name: alloy-sidecar
        image: grafana/alloy:latest
        args:
        - "run"
        - "/etc/alloy/config.alloy"
        - "--server.http.listen-addr=0.0.0.0:12345"
        ports:
        - name: otlp-grpc
          containerPort: 4317
          protocol: TCP
        - name: otlp-http
          containerPort: 4318
          protocol: TCP
        - name: alloy-http
          containerPort: 12345
          protocol: TCP
        volumeMounts:
        - name: alloy-config
          mountPath: /etc/alloy
        - name: podinfo
          mountPath: /etc/podinfo
          readOnly: true
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "384Mi"
            cpu: "300m"